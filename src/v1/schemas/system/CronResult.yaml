CronResult:
  type: object
  properties:
    recurring_transactions:
      type: array
      items:
        $ref: '#/components/schemas/CronResultRow'
    auto_budgets:
      type: array
      items:
        $ref: '#/components/schemas/CronResultRow'
    exchange_rates:
      type: array
      items:
        $ref: '#/components/schemas/CronResultRow'
    bill_notifications:
      type: array
      items:
        $ref: '#/components/schemas/CronResultRow'
    webhooks:
      type: array
      items:
        $ref: '#/components/schemas/CronResultRow'
